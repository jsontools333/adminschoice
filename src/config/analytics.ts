// Central analytics configuration.
// Paste your Google Analytics 4 Measurement ID below (format: G-Q2WMDR5DNY).
// Set `enabled: false` to instantly remove analytics site-wide.
//
// While the ID is the placeholder, NO analytics code loads — so this is safe
// to commit before you've created your GA4 property.

export const ANALYTICS = {
  enabled: true,
  // Your GA4 Measurement ID from analytics.google.com → Admin → Data Streams.
  measurementId: 'G-Q2WMDR5DNY',
};
